# Hugo build output & caches
/public/
/resources/
.hugo_build.lock

# OS / editor
.DS_Store

# Note: static/covers/ IS committed — it's the cached cover art the site serves.

# transient colour-sampling temp file
static/covers/_tmp_color.png

# auto-update log
update.log
